apiVersion: score.dev/v1b1
metadata:
  name: my-project

service:
  ports:
    api:
      port: 80
      targetPort: 8080
containers:
  api:
    image: debian
    args: ["/bin/sh", "-c", "sleep 2600"]
  worker:
    image: debian
    args: ["/bin/sh", "-c", "sleep 2600"]

